<%= simple_form_for(@admin_resource, :html => { :multipart => true }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.file_field :asset %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
